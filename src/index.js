import React from 'react';
import ReactDOM from 'react-dom/client';
import CalendarioAI from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CalendarioAI />
  </React.StrictMode>
);